package net.abitmorehex.blocks.tileentities;

import net.minecraft.block.entity.SignBlockEntity;
import net.minecraft.block.BlockState;
import net.minecraft.util.math.BlockPos;

public class EdifiedSignBlockEntity extends SignBlockEntity {
    public EdifiedSignBlockEntity(BlockPos pos, BlockState state) {
        super(pos, state);
    }
}
